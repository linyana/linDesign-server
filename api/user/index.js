const {
    searchAccount,
} = require('./login')

const {
    userRegister,
} = require('./register')

const {
    getUserDetail,
} = require('./getUserDetail')

module.exports = {
    searchAccount,
    userRegister,
    getUserDetail,
}