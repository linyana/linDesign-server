const {
    searchAccount,
    userRegister,
} = require('./user')

const {
    getCommentList,
    addComment,
} = require('./comment')

module.exports = {
    searchAccount,
    userRegister,
    getCommentList,
    addComment,
}