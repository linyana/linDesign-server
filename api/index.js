const {
    searchAccount,
    userRegister,
} = require('./user')

const {
    getCommentList,
} = require('./comment')

module.exports = {
    searchAccount,
    userRegister,
    getCommentList,
}