const {
    userLoginApi,
} = require('./login')

const {
    userRegisterApi,
} = require('./register')

const {
    getUserDetailApi,
} = require('./getUserDetail')

module.exports = {
    userLoginApi,
    userRegisterApi,
    getUserDetailApi,
}